import React from "react"

import "./logo.styles.css"

const Logo = _ => {
    return(
        <div className="app-logo">
            <span className="logo-text">Swype</span>
        </div>
    )
}

export default Logo